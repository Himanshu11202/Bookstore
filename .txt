git config --global user.name "name"
git config --global user.email "patidarhimanshu2004@gmail.com"
git config --global core.editor "code--wait"
git config --global core.autocrlf "input"
check--
git config --global -e


git intallation


git --version

stages
U - untracked
A - added or staged
C - commited

commands you need to know -
git status -s => to know current status of unstaged files
git log --oneline => to know current status of saved points


  <===commands==>

  clone & status

  clone==>
      git clone <- some link ->  {ise haapne github pr bnaya project vscode pr chla sakte}